@model IEnumerable<Carsport.Common.Models.Conversation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserIdOne)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserIdTwo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UserIdOne)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UserIdTwo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ConversationId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ConversationId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ConversationId })
        </td>
    </tr>
}

</table>
